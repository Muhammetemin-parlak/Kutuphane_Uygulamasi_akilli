@model KutuphaneProjesi.Models.LoginViewModel

@{
    ViewData["Title"] = "Giriş Yap";
}

<div class="container">
    <div class="row justify-content-center align-items-center" style="min-height: 70vh;">
        <div class="col-md-5 col-lg-4">
            <div class="card shadow-lg border-0">
                <div class="card-header text-center text-white py-4" style="background: linear-gradient(135deg, #DC0000 0%, #FF3838 100%);">
                    <i class="bi bi-box-arrow-in-right" style="font-size: 3rem;"></i>
                    <h3 class="mt-3 mb-0 fw-bold">Giriş Yap</h3>
                </div>
                <div class="card-body p-4">
                    <form asp-action="Login" method="post">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                        <div class="mb-4">
                            <label asp-for="Email" class="form-label fw-bold">
                                <i class="bi bi-envelope me-2"></i>E-Posta Adresi
                            </label>
                            <input asp-for="Email" class="form-control form-control-lg" placeholder="ornek@mail.com" />
                            <span asp-validation-for="Email" class="text-danger small"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="Password" class="form-label fw-bold">
                                <i class="bi bi-lock me-2"></i>Şifre
                            </label>
                            <input asp-for="Password" type="password" class="form-control form-control-lg" placeholder="••••••••" />
                            <span asp-validation-for="Password" class="text-danger small"></span>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-lg text-white fw-bold" style="background: linear-gradient(135deg, #DC0000 0%, #FF3838 100%);">
                                <i class="bi bi-box-arrow-in-right me-2"></i>Giriş Yap
                            </button>
                        </div>
                    </form>
                </div>
                <div class="card-footer text-center bg-light py-3">
                    <span class="text-muted">Hesabın yok mu? </span>
                    <a asp-action="Register" class="text-decoration-none fw-bold" style="color: #FF3838;">Hemen Kayıt Ol</a>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}